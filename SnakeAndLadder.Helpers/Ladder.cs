namespace SnakeAndLadder.Helpers
{
    public class Ladder: Range
    {
        public Ladder(int startPoint, int endPoint)
        {
            StartPoint = startPoint;
            EndPoint = endPoint;
        }
    }
}
